import { OnDemandPreloadStrategy } from './on-demand-preload-strategy';

describe('OnDemandPreloadStrategy', () => {
  it('should create an instance', () => {
    expect(new OnDemandPreloadStrategy()).toBeTruthy();
  });
});
