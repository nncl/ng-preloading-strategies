import { NetworkAwarePreloadStrategy } from './network-aware-preload-strategy';

describe('NetworkAwarePreloadStrategy', () => {
  it('should create an instance', () => {
    expect(new NetworkAwarePreloadStrategy()).toBeTruthy();
  });
});
